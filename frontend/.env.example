# ── Frontend API Token ────────────────────────────────────────────────────────
# Must match API_SECRET_TOKEN in backend/.env
# Leave EMPTY if backend auth is disabled (default local dev mode).
VITE_API_TOKEN=
